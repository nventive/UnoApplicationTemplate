<Page x:Class="ApplicationTemplate.Views.Content.WelcomePage"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:local="using:ApplicationTemplate"
	  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	  xmlns:uc="using:Nventive.View.Converters"
	  xmlns:u="using:Nventive.View.Controls"
	  mc:Ignorable="d">

	<Grid Background="{StaticResource MaterialBackgroundBrush}">

		<Grid.RowDefinitions>
			<RowDefinition Height="0.9*" />
			<RowDefinition Height="0.1*" />
		</Grid.RowDefinitions>

		<StackPanel HorizontalAlignment="Center"
					VerticalAlignment="Center"
					Grid.Row="0">
			<TextBlock Text="Dad's app jokes"
					   x:Uid="Home_Title"
					   Style="{StaticResource MaterialHeadline4}"
					   HorizontalAlignment="Center"
					   Margin="0,0,5,0"
					   FontSize="34"
					   FontWeight="Bold" />
			<TextBlock Text="I used to be addicted to soap, but I'm clean now."
					   x:Uid="Home_Text"
					   Style="{StaticResource MaterialBody2}" />
			<Button Content="Welcome"
					Command="{Binding NavigateToOnboarding}"
					VerticalAlignment="Bottom"
					HorizontalAlignment="Center"
					Margin="0,20,0,40" />
		</StackPanel>
	</Grid>
</Page>
