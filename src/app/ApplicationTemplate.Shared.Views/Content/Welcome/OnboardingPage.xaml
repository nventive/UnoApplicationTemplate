<Page x:Class="ApplicationTemplate.Views.Content.OnboardingPage"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:uc="using:Nventive.View.Converters"
	  xmlns:u="using:Nventive.View.Controls"
	  xmlns:toolkit="using:Uno.UI.Toolkit">

	<Page.Resources>
		<ResourceDictionary>
			<x:String x:Key="SearchIcon">ms-appx:///Assets/Search_Icon.png</x:String>
		</ResourceDictionary>
	</Page.Resources>

	<Grid Background="{ThemeResource BackgroundBrush}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<!-- CommandBar -->
		<CommandBar Content="CommandBar"
					Grid.Row="0">
			<CommandBar.PrimaryCommands>
				<!-- Search Button -->
				<AppBarButton>
					<AppBarButton.Icon>
						<BitmapIcon UriSource="{StaticResource SearchIcon}"
									Foreground="{ThemeResource SeparatorBrush}" />
					</AppBarButton.Icon>
				</AppBarButton>
			</CommandBar.PrimaryCommands>
		</CommandBar>

		<TextBlock Text="TEST"
				   Grid.Row="1"/>
	</Grid>
</Page>
